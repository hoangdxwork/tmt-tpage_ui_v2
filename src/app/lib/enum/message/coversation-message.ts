export enum ConversationType {
  All = 1,
  Comment = 2,
  Message = 3
};

export enum ActivityStatus {
  wait,
  sending,
  success,
  fail
}

export enum SendMessageType {
  Message,
  Like,
  Icon,
  Images,
  Product,
  QuickMessage
}
